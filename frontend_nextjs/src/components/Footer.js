import styles from "@/styles";

export default function Footer() {
  return (
    <div className={styles.footerContainer}>
      <span className={styles.footerText}>😀 robertpengcode 2023</span>
    </div>
  );
}
